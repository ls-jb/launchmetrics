# Dependências de PRODUÇÃO (Vercel serverless).
# Para desenvolvimento local, instale também o requirements-dev.txt.

# Framework
fastapi>=0.115

# Banco de dados — psycopg3 (async). asyncpg hanga no handshake SSL com o
# pooler do Supabase em ambiente serverless (Vercel/Lambda). psycopg é estável.
sqlalchemy[asyncio]>=2.0.36
psycopg[binary]>=3.2

# Validação e configuração
pydantic>=2.11
pydantic-settings>=2.7
python-dotenv>=1.0

# Autenticação
PyJWT>=2.10
